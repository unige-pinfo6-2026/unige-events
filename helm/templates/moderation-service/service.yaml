apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: moderation-service
spec:
  type: ClusterIP
  selector:
    app: moderation-service
  ports:
    - port: 8080
      targetPort: 8080
