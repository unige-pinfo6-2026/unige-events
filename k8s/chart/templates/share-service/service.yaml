apiVersion: v1
kind: Service
metadata:
  name: share-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: share-service
spec:
  type: ClusterIP
  selector:
    app: share-service
  ports:
    - port: 8080
      targetPort: 8080
