apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: report-service
spec:
  type: ClusterIP
  selector:
    app: report-service
  ports:
    - port: 8080
      targetPort: 8080
