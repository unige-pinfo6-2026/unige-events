apiVersion: v1
kind: Service
metadata:
  name: comment-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: comment-service
spec:
  type: ClusterIP
  selector:
    app: comment-service
  ports:
    - port: 8080
      targetPort: 8080
