apiVersion: v1
kind: Service
metadata:
  name: follow-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: follow-service
spec:
  type: ClusterIP
  selector:
    app: follow-service
  ports:
    - port: 8080
      targetPort: 8080
