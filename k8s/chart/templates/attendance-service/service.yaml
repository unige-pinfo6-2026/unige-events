apiVersion: v1
kind: Service
metadata:
  name: attendance-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: attendance-service
spec:
  type: ClusterIP
  selector:
    app: attendance-service
  ports:
    - port: 8080
      targetPort: 8080
