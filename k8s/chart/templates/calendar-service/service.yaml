apiVersion: v1
kind: Service
metadata:
  name: calendar-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: calendar-service
spec:
  type: ClusterIP
  selector:
    app: calendar-service
  ports:
    - port: 8080
      targetPort: 8080
