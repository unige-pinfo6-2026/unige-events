apiVersion: v1
kind: Service
metadata:
  name: co-organizer-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: co-organizer-service
spec:
  type: ClusterIP
  selector:
    app: co-organizer-service
  ports:
    - port: 8080
      targetPort: 8080
