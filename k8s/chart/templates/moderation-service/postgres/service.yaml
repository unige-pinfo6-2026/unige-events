apiVersion: v1
kind: Service
metadata:
  name: postgres-moderation
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: postgres-moderation
  ports:
    - port: 5432
      targetPort: 5432
