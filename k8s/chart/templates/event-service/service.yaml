apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: event-service
spec:
  type: ClusterIP
  selector:
    app: event-service
  ports:
    - port: 8080
      targetPort: 8080
