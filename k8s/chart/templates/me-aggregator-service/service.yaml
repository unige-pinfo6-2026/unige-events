apiVersion: v1
kind: Service
metadata:
  name: me-aggregator-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: me-aggregator-service
spec:
  type: ClusterIP
  selector:
    app: me-aggregator-service
  ports:
    - port: 8080
      targetPort: 8080
