apiVersion: v1
kind: Service
metadata:
  name: engagement-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: engagement-service
spec:
  type: ClusterIP
  selector:
    app: engagement-service
  ports:
    - port: 8080
      targetPort: 8080
