apiVersion: v1
kind: Service
metadata:
  name: postgres-engagement
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: postgres-engagement
  ports:
    - port: 5432
      targetPort: 5432
