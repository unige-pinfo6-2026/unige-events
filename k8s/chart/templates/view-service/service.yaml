apiVersion: v1
kind: Service
metadata:
  name: view-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: view-service
spec:
  type: ClusterIP
  selector:
    app: view-service
  ports:
    - port: 8080
      targetPort: 8080
