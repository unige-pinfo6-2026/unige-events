<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.unige.events</groupId>
        <artifactId>parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <properties>
        <sonar.projectKey>unige-pinfo6-2026_unige-events-shared-domain-enums</sonar.projectKey>
        <sonar.projectName>unige-events-shared-domain-enums</sonar.projectName>
    </properties>

    <!--
      shared-domain-enums — 9 cross-projected enums (EventStatus,
      EventCategory, Faculty, AttendanceStatus, CoOrganizerStatus,
      FollowStatus, RecurrenceFrequency, ReportReason, ReportStatus).
      Cf. completion-spec Décision D (REFACTOR-003). Each enum was
      previously duplicated 5–8× across services.

      Risk: any divergence in @Enumerated(STRING) values across
      services would break Hibernate hydration. The sentinel test
      EnumValuesSentinelTest pins the exact values to prevent silent
      drift.
    -->

    <artifactId>shared-domain-enums</artifactId>
    <packaging>jar</packaging>
    <name>UNIGE Events — shared-domain-enums</name>

    <dependencies>
        <dependency><groupId>org.junit.jupiter</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin><artifactId>maven-compiler-plugin</artifactId></plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution><id>prepare-agent</id><goals><goal>prepare-agent</goal></goals></execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals><goal>report</goal></goals>
                        <configuration><outputDirectory>${project.build.directory}/jacoco-report</outputDirectory></configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
                <version>3.2.2</version>
                <executions><execution><id>make-index</id><goals><goal>jandex</goal></goals></execution></executions>
            </plugin>
        </plugins>
    </build>
</project>
