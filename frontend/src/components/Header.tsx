import Navbar from "./Navbar";

export default function Header() {
  return (
    <header className="sticky top-0 left-0 right-0 z-40 bg-background/10 backdrop-blur-sm">
        <Navbar/>
    </header>
  )
}